name: survival
version: '${version}'
main: net.gahvila.survival.survival
description: Lisäosa Gahvilan survival palvelimelle.
api-version: '1.21'
dependencies:
  server:
    GahvilaCore:
      required: true
      load: BEFORE
    WorldGuard:
      required: true
      load: BEFORE
      join-classpath: true
    CrashClaim:
      required: false
      load: BEFORE
      join-classpath: true
